"""Paper-facing analysis scripts (verbatim from the submitted base, path-parameterised).

Provenance: AgentVerse `code/coupling/scripts/` at commit b4a2db6e, which is a
byte-identical extraction of the submitted run's base be9c47c9. The only edits in
this release are the RELEASE PATCH blocks that replace author-private absolute
default paths with repository-relative, env-overridable ones. See docs/PROVENANCE.md.
"""
